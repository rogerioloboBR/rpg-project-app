import React from 'react';
import Header from '../components/Header';

const AddUser: React.FC = () => (
  <div>
    <Header />
    <main>
      <h2>Add User</h2>
      <p>Form to add a new user.</p>
    </main>
  </div>
);

export default AddUser;

import React from 'react';
import Header from '../components/Header';

const AddEvent: React.FC = () => (
  <div>
    <Header />
    <main>
      <h2>Add Event</h2>
      <p>Form to add a new event.</p>
    </main>
  </div>
);

export default AddEvent;
